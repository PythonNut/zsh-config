#compdef c-fu
#
# For https://github.com/thrig/sial.org-scripts/tree/master/c-fu

# just the options, completing arbitrary c code would be tricky
_arguments -s \
  '(--show-code -l)'{--show-code,-l}'[show code]' \
  '(--dry-run -n)'{--dry-run,-n}'[do nothing]' \
  '*'{--defines=,-d}'[defines]:define:' \
  && return 0
